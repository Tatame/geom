#include<stdio.h>
#include<stdlib.h>
#define NULL 0.0000000000
int main(){
    int n=0,s=0;
    double x=0, r=0;
    scanf("%d",&n);
    double**a=(double**)malloc(n*sizeof(double*));
    for (int i=0; i<n; i++){
        a[i]=(double*)malloc(n*sizeof(double));
    }
    for (int i=0; i<n; i++){
        for (int j=0; j<n; j++){
            scanf("%lf",&a[i][j]);
        }
    }
    /////////////////////////////////////////////////////////////
    int nul=0;
    for (int i=0; i<n; i++){
        for (int i=0; i<n; i++){
            nul=0;
            for (int j=i; j<n; j++){
                if (a[j][i]!=NULL) {
                    nul=1;
                    break;
                }
            }
        }
            if (nul){
                if (a[i][i]!=NULL){
                    for (int j=i+1; j<n;j++){
                        x=a[j][i]/a[i][i];
                        for (int l=i; l<n; l++){
                            a[j][l]=a[j][l]-x*a[i][l];
                        }
                    }
                }
                else {
                    s=i;
                    while ((a[s][i]==NULL)&&(s<n-1)) s+=1;
                    if (s!=n-1){
                        for (int j=s+1; j<n;j++){
                            x=a[j][i]/a[s][i];
                            for (int l=i; l<n; l++){
                                a[j][l]=a[j][l]-x*a[s][l];
                            }
                        }
                    }
                }
            }
        
    }
    /////////////////////////////////////////////////////////////
    for (int i=0; i<n; i++){
        for (int j=i; j<n; j++){
            if ((a[j][i]!=NULL)&&(j!=i)){
                for (int k=0; k<n; k++){
                    r=a[j][k]; a[j][k]=a[i][k]; a[i][k]=r;
                }
                break;
            }
            if ((a[j][i]!=NULL)&&(j==i)) break;
        }
    }
    /////////////////////////////////////////////////////////////
    /*for (int i=0; i<n; i++){
            nul=0;
            for (int j=i; j<n; j++){
                if (a[i][j]!=NULL) {
                    nul=1;
                    break;
                }
            }
        if (nul){
            if (a[i][i]!=NULL){
                for (int j=i+1; j<n; j++){
                    x=a[i][j]/a[i][i];
                    a[i][j]=a[i][j]-x*a[i][i];
                }
            }
            else{
                s=i;
                while ((a[i][s]==NULL)&&(s<n-1)) s+=1;
                if (s!=n-1){
                    for (int j=s+1; j<n;j++){
                        x=a[i][j]/a[i][s];
                        for (int l=i; l<n; l++){
                            a[l][j]=a[l][j]-x*a[l][s];
                        }
                    }
                }
            }
        }
    }*/
    ////////////////////////////////////////////////////////////////////////
    double det=1;
    for (int i=0; i<n; i++){
        det=det*a[i][i];
    }
    if (det==NULL) det=0;
    //////////////////////////////////////////////////////////////////////////
    printf("Определитель матрицы = %lf",det);
    /*for (int i=0; i<n; i++){
        for (int j=0; j<n; j++){
            printf("%lf ",a[i][j]);
        }
        printf("\n");
    }*/
    return 0;
}
